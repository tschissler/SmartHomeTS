# Define the refresh token
$token = 'your-refresh-token'

# Encode the token to base64
$base64Token = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes($token))

# Create the Kubernetes Secret
kubectl create secret generic bmw-connect-api-token --from-literal=refresh-token=$base64Token