<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Smarthome.App.Components"
             x:Class="Smarthome.App.Components.EnergyBar"
             x:Name="root"
             x:DataType="local:EnergyBar">
    <VerticalStackLayout x:Name="EnergyBarContainer" BindingContext="{x:Reference root}">
        <Grid x:Name="ParentGrid" BindingContext="{x:Reference root}">

        <!--<BoxView
         x:Name="ProgressBoxView"
         BackgroundColor="#4e8752"
         HeightRequest="{Binding BarHeight}"
         HorizontalOptions="Start"
         CornerRadius="{Binding CornerRadius}"
         WidthRequest="{
             Binding Source={x:Reference ParentGrid}, 
             Path=Width, 
             Converter={StaticResource PercentageConverter}, 
             ConverterParameter={Binding Source={x:Reference root}, Path=PVProduction}
         }"
      />-->
        </Grid>
    </VerticalStackLayout>
</ContentView>
