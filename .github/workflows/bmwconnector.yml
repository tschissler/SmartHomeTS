name: BMWConnector CD

on:
  push:
    branches: [ "main" ]
    paths:
      - .github/workflows/bmwconnector.yml
      - BMWConnector/**
      # Shared code this service references (transitively resolved) - without
      # these the image is not rebuilt and the change deploys silently nowhere.
      - SharedContracts/**
      # Added with the service heartbeat (backlog item 3).
      - Libs/HeartbeatLib/**
  workflow_dispatch:

# A newer push supersedes a running build of the same branch.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PROJECT_NAME: BMWConnector
  IMAGE_NAME: bmwconnector

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Build and test
      run: dotnet test ${{ env.PROJECT_NAME }}/${{ env.PROJECT_NAME }}.slnx

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

    - name: Generate version information
      id: version
      run: echo "VERSION=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

    - name: Build and publish multi-architecture Docker image
      uses: docker/build-push-action@v6
      with:
        # Repository root: the image needs SharedContracts alongside the connector.
        context: .
        file: ./${{ env.PROJECT_NAME }}/dockerfile
        platforms: linux/amd64,linux/arm64
        push: true
        # Stamps the image tag into the assembly, which is where the service heartbeat reads
        # the running version from.
        build-args: |
          VERSION=${{ steps.version.outputs.VERSION }}
        tags: |
          tschissler/${{ env.IMAGE_NAME }}:${{ steps.version.outputs.VERSION }}
          tschissler/${{ env.IMAGE_NAME }}:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
        provenance: false
        sbom: false
