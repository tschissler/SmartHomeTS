apiVersion: v1
kind: Service
metadata:
  name: enphaseconnector
  namespace: smarthome
  labels:
    app: enphaseconnector
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: enphaseconnector
