<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:Smarthome.App.Components"
             x:Class="Smarthome.App.Components.BatteryState"
             x:Name="root">
    <VerticalStackLayout x:Name="BatteryStateContainer" BindingContext="{x:Reference root}">
        <Label 
            Text="Quellen und Verbräuche"
            VerticalOptions="Start" 
            HorizontalOptions="Start"
            FontSize="16"/>
        <Label
            Text="{Binding Value, StringFormat='Batterie Ladestand: {0}%'}"
            HorizontalOptions="Center"
            FontSize="10"/>
        <components:StackedProgressBar
            Value="{Binding Value}"
            BarHeight="20"
            />
    </VerticalStackLayout>
</ContentView>
